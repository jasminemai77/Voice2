[package]
name = "voice2-desktop"
version = "0.1.0"
description = "Voice2 local desktop shell"
authors = ["Voice2 contributors"]
license = "Apache-2.0"
edition = "2021"

[lib]
name = "voice2_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

